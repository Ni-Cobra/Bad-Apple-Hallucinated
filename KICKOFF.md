Resume the Bad Apple recreation. Follow the resume protocol: read PROGRESS.md,
verify the last completed work actually exists on disk, then do the next batch
per PROJECT.md (§9 plan, §12 session rules — small chronological batch, respect
the iteration caps). Update PROGRESS.md after each completed unit, and end the
session at a clean checkpoint with a partial preview encode if frames are gapless.